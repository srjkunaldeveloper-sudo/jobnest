import 'package:flutter/material.dart';

class AppRadius {
  AppRadius._();

  static BorderRadius small = BorderRadius.circular(6);

  static BorderRadius medium = BorderRadius.circular(8);

  static BorderRadius large = BorderRadius.circular(12);

  static BorderRadius extraLarge = BorderRadius.circular(16);

  static BorderRadius pill = BorderRadius.circular(100);
}